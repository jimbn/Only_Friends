<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Only_Friends</title>
    {{!-- linking the stylesheet. it works  --}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/modal.css">
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>

<body>
    {{!-- Header --}}
  <header class="container position-sticky top-0">
    <div class="row">
      <div class="col-3">
                <input type="text"/>
                <button>Search</button>
      </div>
      <h1 class="col-6"> <a href="/">Only_Friends</a> </h1>
      <div class="col-2">
        {{#if loggedIn}}
          <a href="/">logout</a>
        {{else}}
          {{!-- <a href="/login">Login</a>
          <a href="/signup">Signup</a> --}}
          <button id="open-login-modal">Log In</button>
          <button id="open-signup-modal">Sign Up</button>
        {{/if}}
      </div>
    </div>
  </header>
  
    {{!-- body gets replaced by the views --}}
  {{{body}}}

{{!-- Modals for login/signup --}}
  {{> login-modal}}
  {{> signup-modal}}

  {{!-- Footer **Will add our contact: thinking of just putting contributers on seperate page and hard code that in the server -Jim **--}}
  <footer>

  </footer>

  {{!-- script tag for logout functionality  --}}
  <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{else}}
  <script src="/javascript/login.js"></script>
  <script src="/javascript/signup.js"></script>
  {{/if}}
</body>

</html>
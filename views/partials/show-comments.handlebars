<div class="card-container">
    {{#each comments as |comment|}}

    <div class="comment ">

        {{user.username}} commented at <span>
        {{comment.created_at}}</span>
 <p class="commentline">
        {{comment.comment_text}}
    </p>
    </div>
   

    {{/each}}
</div>
<div class="card-container">
    {{#each comments as |comment|}}

    <div class ="comment">
        <div>
            {{user.username}}
            <div>
                {{created_at}}
            </div>
        </div>
    </div>
    <div>
        {{coments.comment_text}}
    </div>

    {{/each}}
</div>